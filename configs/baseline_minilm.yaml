data:
  data_cfg: configs/data.yaml

model:
  embedder: sentence-transformers/all-MiniLM-L6-v2  # 384-d embeddings

training:
  max_iter: 200
  C: 0.5
  class_weight: balanced
  threshold: 0.5
  n_jobs: -1
  seed: 42

logging:
  run_name: baseline_minilm_lr
  save_dir: results/runs

